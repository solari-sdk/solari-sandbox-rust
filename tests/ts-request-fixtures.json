{
  "_comment": "GENERATED by tests/sdk/contract.test.ts — do not hand-edit. The exact HTTP requests the REFERENCE SDK (@solarisdk, TypeScript) emits for the shared core surface. The Python, Go, Rust and C++ contract tests each assert their SDK emits byte-identical requests, so this file is the one place the five bindings agree on the wire. Regenerated on every run: if it diffs against the committed copy, the reference SDK's wire shape changed and the other four must follow.",
  "_coreOps": "Every SDK must implement exactly these ops. Each language's contract test asserts its op table covers this list, so adding one here fails all four non-reference suites until they implement it.",
  "sessionId": "pool-a:vm-7:org-42.sig",
  "coreOps": [
    "createSandbox",
    "createDesktop",
    "get",
    "pause",
    "resume",
    "kill"
  ],
  "requests": {
    "createSandbox": {
      "method": "POST",
      "path": "/sandboxes",
      "body": {
        "template": "base",
        "kind": "sandbox",
        "cpu": 2,
        "memMb": 2048,
        "diskGb": 10,
        "envs": {
          "FOO": "bar"
        },
        "metadata": {
          "proj": "alpha"
        },
        "timeoutMs": 600000,
        "fromSnapshot": "snap_seed",
        "lifecycle": {
          "onTimeout": "pause",
          "autoResume": true
        },
        "volumes": [
          {
            "volumeId": "vol_data",
            "path": "/data"
          }
        ]
      }
    },
    "createDesktop": {
      "method": "POST",
      "path": "/sandboxes",
      "body": {
        "template": "default",
        "kind": "desktop",
        "cpu": 2,
        "memMb": 2048,
        "diskGb": 10,
        "envs": {
          "FOO": "bar"
        },
        "metadata": {
          "proj": "alpha"
        },
        "timeoutMs": 600000,
        "fromSnapshot": "snap_seed",
        "lifecycle": {
          "onTimeout": "pause",
          "autoResume": true
        },
        "resolution": "1280x720",
        "record": true,
        "volumes": [
          {
            "volumeId": "vol_data",
            "path": "/data"
          }
        ]
      }
    },
    "get": {
      "method": "GET",
      "path": "/sandboxes/pool-a%3Avm-7%3Aorg-42.sig"
    },
    "pause": {
      "method": "POST",
      "path": "/sandboxes/pool-a%3Avm-7%3Aorg-42.sig/pause"
    },
    "resume": {
      "method": "POST",
      "path": "/sandboxes/pool-a%3Avm-7%3Aorg-42.sig/resume"
    },
    "kill": {
      "method": "DELETE",
      "path": "/sandboxes/pool-a%3Avm-7%3Aorg-42.sig"
    }
  }
}
